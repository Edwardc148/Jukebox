<!DOCTYPE html>
<html>
  <head>
    <link rel="icon" href="https://s3-us-west-1.amazonaws.com/jukebox-storage-dev/users/logo_j.png">
    <title>Jukebox</title>
    <%= csrf_meta_tags %>

    <%= stylesheet_link_tag    'application', media: 'all' %>
    <%= javascript_include_tag 'application' %>

    <% if current_user %>
      <script>
        window.currentUser = {
          "id": <%= current_user.id %>,
          "email": "<%= current_user.email %>",
          "name": "<%= current_user.name %>"
        };
      </script>
    <% end %>
    <script defer src="https://use.fontawesome.com/releases/v5.0.6/js/all.js"></script>
  </head>

  <body>
    <%= yield %>
  </body>
</html>
